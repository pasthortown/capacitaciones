/**
 * Wrapper delgado sobre `lucide-react` para centralizar el uso de íconos.
 * Importa directamente de `lucide-react` cuando necesites un ícono específico;
 * este archivo existe como punto de extensión futura (theming, tamaños).
 *
 * Uso:
 *   import { User } from 'lucide-react';
 *   <User size={20} />
 *
 * Si en el futuro queremos un set de íconos custom, se encapsulan aquí.
 */
export { default as Icon } from 'lucide-react';
