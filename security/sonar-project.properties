# Config principal de Sonar a nivel repo. Este archivo queda como referencia;
# los escaneos reales usan los `sonar-project.properties` dentro de `frontend/`
# (JS) y los parámetros MSBuild de `dotnet-sonarscanner` para backend (.NET).

sonar.organization=capacitaciones
sonar.host.url=http://localhost:9000
